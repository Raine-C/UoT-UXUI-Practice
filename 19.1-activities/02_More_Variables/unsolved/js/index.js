// Create the addNumbers function here.
function addNumbers() {
    var two = 2;
    var three = 3;
    var five = two + three
    console.log(five);
}

addNumbers();

function stringConcat() {
    var tacoVar = 'taco';
    var catVar = 'cat';
    var tacoCat = tacoVar + ' ' + 'cat';
    
    alert(tacoCat);
}

stringConcat();
// Create the stringConcat function here.
